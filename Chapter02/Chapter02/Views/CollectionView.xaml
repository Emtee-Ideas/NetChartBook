<UserControl x:Class="Chapter02.Views.CollectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Chapter02.Views"
             xmlns:model="clr-namespace:Chapter02.Models"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="450">
    <UserControl.DataContext>
        <model:CollectionViewModel/>
    </UserControl.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="55"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DataGrid ItemsSource="{Binding Path=DataCollection}"
                  ColumnWidth="*"
                  CanUserAddRows="False"
                  Grid.Row="0"
                  FontSize="10" Margin="0,0,0,130" Grid.RowSpan="2"/>
        <Button x:Name="btnUpdate" Content="Update"
                Width="100" Height="25" Grid.Row="1"
                Command="{Binding UpdateStock}"/>
    </Grid>
</UserControl>
